#pragma once

using namespace std;

extern bool allTrue(bool* vector, int dimension);

extern void shortestPathsSequential(int* matrix, int dimension, int* results);

//extern void shortestPathsListSequential(int* adiacencyList, int* weightsList, int* offsetList, int dimension, int* results);